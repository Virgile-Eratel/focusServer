import type { VerdictCategory } from '@focus/shared';

/** Libellés (anglais) des catégories — source unique pour popup et pages. */
export const CATEGORY_LABELS: Record<VerdictCategory, string> = {
  adult: 'Adult',
  entertainment: 'Entertainment',
  other: 'Other',
  unknown: 'Unknown',
};
